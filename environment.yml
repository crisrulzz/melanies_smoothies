name: melanies_smoothies
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.8
  - pip
  - pip:
      - streamlit>=1.24.1
      - snowflake-snowpark-python
      - pandas>=2.0
      - numpy<=1.21.6
