name: melanies_smoothies
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10 # Streamlit Cloud compatible
  - pip
  - pip:
      - streamlit>=1.24.1
      - snowflake-snowpark-python
      - pandas>=1.3.0
      - numpy<=1.21.6
      - requests>=2.25.1
